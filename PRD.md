# 港股模拟交易系统 产品需求文档 (PRD)

## 1. 项目概述
本项目旨在开发一款高仿真的港股模拟交易系统。每位参赛者初始将获得 100 万港币虚拟资金。系统基于香港交易所（HKEx）的真实交易规则进行定制与简化，通过对接第三方（AOB）港股行情服务，提供实时撮合、T+2结算及相关的交易风控功能。

## 2. 功能模块设计

### 2.1 用户与资产模块
*   **资金账户**：初始化分配 1,000,000.00 HKD 虚拟资金。账户资金分为：可用资金、冻结资金（委托挂单中）、在途资金（T+2待交收）。
*   **持仓管理**：记录用户持仓标的、持仓均价、持仓数量。不支持孖展交易（融资）和沽空（融券）。

### 2.2 交易标的与行情模块
*   **股票白名单池**：仅允许交易指定的股票池（恒生指数成份股、恒生中国企业指数成份股、恒生香港中资企业指数成份股、指定ETF）。其余股票均不能透过比赛账户进行交易。不支持新股认购。
*   **实时行情接入**：
    *   **HTTP 行情查询**：调用第三方 AOB 接口批量拉取最新行情与昨收盘价。
    *   **MQ 行情订阅**：通过 RabbitMQ 监听 `quote.queue.hk.realTime` 等队列，实时接收按盘价（Nominal Price）更新，用于驱动本地订单撮合。

### 2.3 委托下单模块
*   **交易时段**：
    *   支持时段：交易日 09:30-12:00，13:00-16:00（如遇恶劣天气以港交所公告为准）。
    *   不支持竞价交易时段。
*   **订单类型**：仅支持 **市价盘**（Market Order）和 **限价盘**（Limit Order）。
*   **买卖限制**：
    *   **频次限制**：单日只限最多 20 笔买入交易，卖出交易次数不限。
    *   **轮候限制**：单个账户的限价盘最多只能容纳 5 个轮候指示（排队中）。
    *   **交易单位**：必须以“手”（Lot Size）为单位，不接受碎股买卖。
*   **价格校验**：
    *   限价单：落盘股价必须在当前按盘价的上下 20 个价位之间。
    *   底价限制：买入价下限为 HK$0.05，卖出价下限为 HK$0.01。
    *   市价单：无需输入股价。

### 2.4 模拟撮合与订单生命周期
*   **撮合机制**：
    *   **模拟成交价**：以当时的按盘价为准。
    *   **限价单成交条件**：若买入价 ≥ 当时按盘价，或卖出价 ≤ 当时按盘价，则立即以当时按盘价成交。
    *   **市价单成交条件**：立即以最新按盘价成交。
*   **订单生命周期**：
    *   交易时段内发出的限价单，有效至当日收市；当日未完成则收市后自动取消。
    *   非交易时段发出的限价单，有效至下一个交易日收市。
*   **停牌处理**：停牌期间只可取消未成交挂单，不能下达新订单。

### 2.5 费用与结算模块
*   **交易费用计算**（每笔模拟交易收取）：
    1.  **模拟经纪佣金**：成交金额的 0.25%，最低收费 HK$100。
    2.  **模拟交易处理费**（仅买入收取）：每手 HK$2.5（最低 HK$30，最高 HK$200）。
    3.  **模拟印花税**：成交金额的 0.1%（最低收费 HK$1）。
    4.  **模拟交易征费**：成交金额的 0.003%。
    5.  **模拟交易费**：成交金额的 0.005%。
*   **T+2 结算交收**：模拟成交将于交易日后的第二个营业日（T+2）正式结算。
*   **公司行动处理**：
    *   **不支持**：供股、配股、以股代息等部分公司行动。股息及红股不计入资产。
    *   **私有化/除牌**：若发生除牌或重新挂牌，系统将在除牌日后的第二个营业日（T+2）按最后收市价将虚拟资金强制结转存入账户。

---

## 3. 业务流程图 (核心逻辑流)

### 3.1 下单与撮合流程
1.  **提交委托**：用户选择股票、方向（买/卖）、类型（市价/限价）、价格与数量。
2.  **规则校验**：
    *   验证标的是否在白名单内，股票是否停牌。
    *   验证当前时段，判断订单有效期。
    *   验证单日买入次数（<20）及限价单轮候数量（≤5）。
    *   验证价格上下限及最小变动单位。
    *   验证可用资金/可用持仓是否充足（资金需包含预估手续费）。
3.  **资源冻结**：扣减相应的可用资金或持仓。
4.  **订单入列与撮合**：
    *   系统读取 Redis 或内存中的最新按盘价。
    *   市价单立刻按盘价成交。限价单比对按盘价，符合则成交，不符合则加入轮候队列。
5.  **行情驱动**：通过 RabbitMQ 实时消费 `quote.queue.hk.realTime` 推送，当最新价变动时，触发轮候队列复算。
6.  **清算入账**：成交后精确计算5项费用，扣除后生成交割单。
7.  **收市清理**：16:00后，将当日未成交限价单置为已撤销，解冻对应资源。

---

## 4. API 接口规范

### 4.1 外部依赖接口 (对接 AOB)
*   **批量拉取行情**：
    *   `POST ${AOB_HOST}/aob/quotation/security/batchDetail`
    *   用于获取股票当前状态、按盘价（`price`）、昨收（`prevClose`）等基础信息。
*   **实时行情订阅**：
    *   基于 RabbitMQ AMQP 协议及 Protobuf 序列化。向 `subscribe.exchange` 发送订阅。监听 `quote.queue.hk.realTime` 队列解析 `RespPushBasicQuote` 结构体进行实时撮合驱动。

### 4.2 内部业务 API 设计 (RESTful JSON)

#### 1. 账户资产查询
*   **Endpoint**: `GET /api/v1/account/assets`
*   **Response**: 包含总资产、可用资金、冻结资金、持仓总值等。

#### 2. 提交交易委托
*   **Endpoint**: `POST /api/v1/trade/order`
*   **Payload**:
    ```json
    {
      "assetId": "00700.HK",
      "direction": "BUY",      // BUY | SELL
      "orderType": "LIMIT",    // LIMIT | MARKET
      "price": 300.20,         // 限价单必填
      "quantity": 100          // 必须为该股票 lotSize 的整数倍
    }
    ```

#### 3. 撤销委托
*   **Endpoint**: `POST /api/v1/trade/order/{orderId}/cancel`
*   **Response**: 释放相关冻结资金或持仓。

#### 4. 查询当前轮候挂单
*   **Endpoint**: `GET /api/v1/trade/orders/active`
*   **Response**: 返回当前排队中的未成交限价单（最多5条）。

#### 5. 查询交割单与持仓
*   **Endpoint**: `GET /api/v1/trade/positions` (持仓) / `GET /api/v1/trade/history` (历史成交)

---

## 5. 验收标准 (Acceptance Criteria)

### 5.1 交易时段与标的
*   **AC1**: 尝试对非白名单股票或碎股下达委托时，系统明确拦截并报错。
*   **AC2**: 竞价时段（如09:00-09:30）系统不接受订单，且不受理非交易时间的市价单。

### 5.2 交易风控限制
*   **AC3**: 用户当日第 21 笔买单提交时，系统拒绝并提示超限。卖单无此限制。
*   **AC4**: 用户当前已有 5 个排队限价单时，尝试提交第 6 个限价单应被系统拒绝。
*   **AC5**: 限价单价格超出当前按盘价 ±20 个价位，或者买入价低于 HK$0.05 / 卖出价低于 HK$0.01 时，订单提交失败。

### 5.3 撮合与费率计算
*   **AC6**: 撮合成交价严格取当前行情的按盘价，买价≥按盘价 或 卖价≤按盘价时触发。
*   **AC7**: 验证一笔买入交易，各项费率必须符合公式（如：处理费按手计算且在 HK$30-200 之间，印花税 0.1% 最低 HK$1，佣金最低 HK$100）。
*   **AC8**: 日终 16:00 后，所有交易时段内未成交的限价单自动标记为已取消，资金或持仓解冻。

### 5.4 结算交收机制
*   **AC9**: 用户的模拟成交记录将在 T+2 营业日转换为正式到账/扣款，在此之前表现为在途状态。
*   **AC10**: 遇到标的除牌，系统能在 T+2 营业日按最后收市价将对应的持仓价值折算为虚拟资金退回给用户。

---

## 6. 技术架构与工程规范

### 6.1 前端要求
*   **技术栈**：Next.js (App Router), TypeScript, Tailwind CSS
*   **包管理**：npm
*   **工程目录**：所有前端代码与配置文件需直接生成在根目录 `frontend/` 下。
*   **UI规范**：严格参照本地设计稿 `/Users/jimmy/AI/UI设计稿` 进行开发。

### 6.2 后端要求
*   **核心框架**：Spring Boot, Mybatis, Mybatis-plus, Java 17
*   **依赖管理**：Maven
*   **中间件**：RabbitMQ（行情与异步任务）、Redis（缓存与高速撮合）、MySQL（持久化）
*   **工程目录**：所有后端代码与配置文件需直接生成在根目录 `backend/` 下。
