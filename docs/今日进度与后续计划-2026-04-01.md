# 港股模拟交易系统今日进度与后续计划（2026-04-01）

## 1. 今日结论

- 当前项目已经达到“可联调、可演示、核心交易链路可跑”的阶段，不再只是静态高保真原型。
- 在“不接真实行情、暂不处理内网 AOB 接入”的前提下，后续工作重点应转向稳定性、自动化验证、规则补齐和文档收口。
- 本文件仅补充今日判断，不修改 `2026-03-30` 与 `2026-03-31` 的既有记录。

## 2. 今日复验结果

### 2.1 前端

- 命令：`cd frontend && npm run build`
- 结果：通过
- 说明：当前 Next.js App Router 页面已可稳定构建，生成 21 个页面路由。

### 2.2 后端

- 命令：
  - `export JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home`
  - `export PATH="$JAVA_HOME/bin:$PATH"`
  - `cd backend && mvn test`
- 结果：通过
- 说明：当前后端全量测试已通过，最新结果为 `Tests run: 124, Failures: 0, Errors: 0, Skipped: 0`。

### 2.3 前端现有测试现状

- 当前仓库已存在可执行的前端 adapter 单测：
  - `frontend/lib/adapters/guest-info.test.ts`
  - `frontend/lib/adapters/leaderboard-list.test.ts`
  - `frontend/lib/adapters/ranking.test.ts`
- 这些测试基于 `node:test`，当前可以直接通过 `node --test lib/adapters/*.test.ts` 跑通。
- 但仓库此前尚未将其收口为统一 `npm run test` 入口，因此仍不能视为完整门禁。

## 3. 当前已具备能力

### 3.1 前端

- 登录、注册资料补全、首页、个人、记录、交易、排行榜、更多等主要页面已具备。
- 多语言 `zh-Hant / zh-Hans / en` 已接通。
- 页面数据读取与提交已开始收口到 `frontend/lib/adapters/` 与 `frontend/lib/api/`。
- 交易详情页已接入快照 + SSE 增量更新的前端消费链路。

### 3.2 后端

- 登录验证码、用户资料、头像上传、账户信息、排行榜、订单查询、下单、改单、撤单、预览等 `/api/v1` 主链路已具备。
- 手续费、撮合、T+2、交易日历、结算调度、多语言错误信息已具备基础实现。
- 多语接口与三语交易回归脚本已具备基础支撑。

## 4. 当前不纳入本阶段的事项

- 真实行情接入
- 内网 AOB 联调
- 真正生产级 MQ 行情闭环验证

说明：

- 由于真实行情链路当前受内网限制，本阶段默认不将其作为阻塞项。
- 所有后续排期均以“保留当前 mock / realtime stub 方案”作为前提。

## 5. 现在最值得继续做的事

### P0

1. 前端补齐自动化入口
   - 建立稳定 `npm run test`
   - 后续再补 `npm run lint`
   - 固定前端最小门禁为 `test + build`

2. 后端 fallback 策略收口
   - 明确哪些环境允许 `in-memory fallback`
   - 明确哪些环境必须开启 strict mode
   - 补齐 strict / relaxed 两种模式的测试覆盖

3. 文档收口
   - 统一最新测试数
   - 统一“前端已有测试文件但缺少统一入口”的表述
   - 明确“真实行情暂缓，不作为当前阻塞项”

4. 回归链路固化
   - 固定每日回归命令
   - 扩充登录态、资料完成态、排行榜、错误提示等 smoke 范围

### P1

1. 交易规则细项补齐
   - 半日市
   - 临时停市
   - 竞价阶段不支持的明确拦截
   - 除牌 / 私有化 T+2 强制结转

2. 前端四态与失败体验补漏
   - loading
   - empty
   - error
   - success

3. 更多展示页从静态展示逐步切到现有 `/api/v1`

### P2

1. 最小 CI 门禁
2. 废弃页面 / 兼容接口清理策略
3. 文档与代码持续对齐机制

## 6. 建议执行顺序

1. 先补前端 `npm run test`
2. 再补前端 `npm run lint`
3. 再收口后端 strict / fallback 策略与测试
4. 再补交易规则细项和前端状态补漏
5. 最后建立最小 CI

## 7. 今日之后的阶段目标

在不接真实行情的情况下，把项目推进到以下状态：

- 前端：`npm run test`、`npm run build` 可稳定执行
- 后端：`mvn test` 可稳定执行，strict / relaxed 行为有明确测试覆盖
- 文档：最新状态与代码一致
- 业务：登录、资料、下单、改单、撤单、记录、排行榜链路可稳定演示
